import React from "react";

type Props = {};

export const Logo: React.FC<Props> = (props): JSX.Element => {
  return (
    <div className="logo">
      <h1>
        devise<span>STUDIO</span>
      </h1>
    </div>
  );
};
